let config = { // The configurations for the size of the window.
    type: Phaser.AUTO, // The graphics context to render the game in, Phaser will choose for you since it's set to AUTO
    width: 640,
    height: 480
  }

let game = new Phaser.Game(config) // Sets up the new phaser game.